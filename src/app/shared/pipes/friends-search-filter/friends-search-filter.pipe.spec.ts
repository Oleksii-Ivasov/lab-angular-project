import { FriendsSearchFilterPipe } from './friends-search-filter.pipe';

describe('FriendsSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FriendsSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
